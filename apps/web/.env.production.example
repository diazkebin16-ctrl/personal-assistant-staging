# Server-only deployment values. Backend/public origins must use HTTPS in production.
WEB_PUBLIC_ORIGIN=
WEB_BACKEND_ORIGIN=
WEB_SUPABASE_URL=
# This is Supabase public anon/publishable configuration, never a service-role key.
WEB_SUPABASE_ANON_KEY=
WEB_ENABLE_HSTS=true
PORT=3000
HOST=127.0.0.1
