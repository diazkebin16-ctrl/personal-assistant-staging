# AI Router interface status

Phase 5 registers no public AI completion, prompt proxy, provider selection, model override, or
usage-query endpoint. The Router is an internal service for the future Orchestrator.

Internal inputs contain routing metadata and identity but no ownership field supplied by a client.
Provider input is ephemeral and bounded. Public API expansion requires a later approved architecture
and must not expose arbitrary model/provider authority.

