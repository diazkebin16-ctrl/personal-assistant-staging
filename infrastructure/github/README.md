# GitHub foundation

GitHub is the intended canonical source. Pull-request CI validates formatting, lint, strict typing,
Alembic migration round-trips, and the complete pytest suite. Weekly Dependabot pull requests cover
Python and GitHub Actions dependencies. Auto-merge is not enabled.
